# Cross-Stage Pending Contracts

This file records contracts that must not be silently papered over by later branches.

The former external Foundation blocker `ENSH-CI-ACTIONS-001` is closed: hosted runners resumed, workflow `33165771852` / job `98830694040` executed the exact final Foundation implementation HEAD and completed all committed gates GREEN before PR #2 merged.

`ENSH-L1-CORE-TO-TERRAIN-001` is closed by Stage 01 + Stage 02 executable evidence. PR #16 (`feat/02-materialization-rules`) merged as `3a0ea59cb548d373b4b181f2c4bd2e77bd9ff925` after exact-head workflow `33227119616` / job `99032887579` completed GREEN. Materialization re-samples canonical `ShroudQuery` immediately before mutation, stale queued work is rejected, and no independent terrain-spread state exists.

`ENSH-L1-CORE-TO-EXPOSURE-001` is closed by Stage 01 + Stage 03 executable evidence. PR #22 (`feat/03-player-exposure-state`) merged as `73396291680aebe9d45e7f6d6347579d04010dd1` after exact PR-head workflow `33246650603` / job `99085167187` completed GREEN. Player exposure samples the same canonical `DefaultShroudQuery`/`ShroudSample` field used by terrain/client state, preserves Sanctuary suppression as an effective overlay and owns no independent Shroud severity state.

`ENSH-L1-FLAME-WARD-001` is closed by Foundation + Stage 02 + Stage 03 + Stage 05 executable evidence. Foundation owns the sole `FlameWardQuery` boundary; Stage 02 PR #15 proves shared `DefaultMutationAuthority` semantics for `CORRUPTION`, `CORE_PLACEMENT`, `PURIFICATION` and normal authorization; Stage 03 PR #22 proves suppressed SHROUD/DEADLY samples recover exposure without erasing latent logical severity/intensity/source; Stage 05 PR #38 supplies the real indexed Sanctuary provider and physical Flame Altar lifecycle through the same runtime handle. Exact final Stage 05 workflow `33303582593` / run #1016 completed GREEN on HEAD `89c6346b4ed74af131b434662940ad9e0e171858` before merge `35fd0f47239646b2df84cda2989126ad432e7fd4`, including GameTests, two-boot reload and dedicated-server smoke. No second ward authority or logical Shroud field exists.

`ENSH-L1-LICH-REWARD-001` is closed by Stage 06.03 + 06.04 executable evidence. Stage 06.03 PR #46 established provider-neutral defeat of the exact marked encounter actor without issuing a reward early. Stage 06.04 PR #48 (`feat/06-lich-skull`) owns the authentic component-backed `enshrouded:lich_skull_manifestation_1`, validates the stored encounter/manifestation identity, preserves external-provider loot, rejects unstamped/lookalike skulls, and binds the item to the generic Stage 05 ritual through `enshrouded:lich_manifestation_1`. Reward issuance is encounter-ID scoped and exactly-once: `rewardIssued=true` is committed only after physical item-entity insertion succeeds, while failed delivery remains retryable. Replay and real two-boot reload emit no additional reward. Final exact PR-head `b7a90c52c3c11b17958d83adfb5554996e29140b` completed workflow `33348592278` / job `99357385794` GREEN across unit tests, build/JAR checks, 74/74 GameTests, SavedData two-boot reload and dedicated-server save/reload smoke before merge `368f30c710246580e47e262462118f8b9e4a03ea`.

## Magic classification boundary — Stage 04 core + Stage 08.02 adapters complete

- `ENSH-L1-MAGIC-CLASSIFY-001` is closed. Stage 04 PR #28 (`feat/04-magic-resistance`) established the standalone Foundation-owned side: `DefaultMagicDamageClassifier` consumes canonical damage-type tags, `enshrouded:magic` bridges `#neoforge:is_magic`, `MagicResistanceService` is the only reducer, and `UNKNOWN`/physical damage remains unchanged. Exact Stage 04 PR-head workflow/job `33280436245` / `99174583880` completed GREEN before merge `2a7b83ef16645ed918d9e8d642471513a89d25a5`. Stage 08.02 PR #61 (`feat/08-magic-systems`) completes the optional-adapter side without replacing that baseline: Ars Nouveau and Iron's Spells publish their current spell/school damage types through `#neoforge:is_magic`, while narrow exact-ID adapters provide only fallback/evidence through `CompositeMagicDamageClassifier`. Arbitrary namespace IDs and current non-school/effect damage remain `UNKNOWN`; explicit `magic_bypass` remains authoritative; contradictory strongest evidence fails closed. The adapters register no event hook and mutate no damage, so the existing single `LivingDamageEvent.Pre` path invokes `MagicResistanceService` exactly once on the final `MagicDamageClassification`. Final implementation HEAD `85ba070753031d6b8e41351b2cdc5055a34d47d7` completed exact PR-head workflow/job `33539127624` / `99960767541` GREEN across the full gate set before merge `916ccf16c10fc521c89475f7cbd67e6efbe81751`; independent post-merge `main` workflow/job `33539723280` / `99962689287` also completed GREEN. No second magic authority or reducer remains open.

## Multiplayer ownership and protection boundary — Stage 08.03 complete

- `ENSH-L1-FLAME-PASSAGE-001` is closed. Foundation owns `ProgressionOwnerResolver` and `FlamePassageQuery`; Stage 03 PR #24 proves the DEADLY consumer uses only those interfaces and Stage 05 PR #30 proves the persistence-backed owner-scoped Flame/passage provider through the same runtime binding. Stage 08.03 PR #64 adds optional FTB Teams substitution through that exact resolver boundary: `integrations.ftbTeamsSharedProgression` defaults false, personal teams retain PLAYER ownership, shared/party teams resolve to stable TEAM keys, and resolver changes affect only future operations. No player↔team progression migration or second store is performed. Final implementation HEAD `054eed2ab50f0ac755d98748d948510d977d6ad9` completed exact PR-head workflow/job `33577174521` / `100083590564` GREEN before merge `7e119ee893801edf3e3ed3856030da78c97edfd1`; independent post-merge `main` workflow/job `33580200835` / `100092676457` also completed GREEN across the complete gate set.

- `ENSH-L1-OWNER-SNAPSHOT-001` is closed. Stage 05 PR #32 proves ritual execution resolves one immutable `ProgressionOwner`; Stage 06 PR #46 persists that exact owner with the encounter and never re-resolves it on defeat; Stage 06 PR #48 derives reward identity from the stored owner. Stage 08.03 completes the membership-change side: executable resolver tests change the FTB Teams result after the original owner snapshot and prove the captured/in-flight key remains unchanged while only a later resolver call observes new membership. No ritual, encounter or reward silently migrates/re-resolves ownership. The closing Stage 08.03 evidence is final HEAD `054eed2ab50f0ac755d98748d948510d977d6ad9`, PR-head workflow/job `33577174521` / `100083590564`, merge `7e119ee893801edf3e3ed3856030da78c97edfd1`, and post-merge `main` workflow/job `33580200835` / `100092676457`, all GREEN.

- `ENSH-L1-CLAIM-SAFETY-001` is closed. Stage 02 PR #15 established the canonical fail-closed tri-state `ProtectedAreaService`/`DefaultMutationAuthority`: `PROTECTED` and `INDETERMINATE` veto by default. Stage 08.03 supplies the real optional-provider side without global scans: FTB Chunks uses indexed `ClaimedChunkManager.getChunk(ChunkDimPos)`, MineColonies uses `IColonyManager.getColonyByPosFromWorld(Level, BlockPos)`, absent providers register no adapter, and loaded API mismatch/query failure becomes bounded-diagnostic `INDETERMINATE`. Final review additionally found and fixed an authority-bypass risk in config-created authorities: runtime claim providers are now composed even when a caller supplies an explicit protection service, covering worldgen and purification paths. The regression RED was PR workflow/job `33576732210` / `100082231972`; final HEAD `054eed2ab50f0ac755d98748d948510d977d6ad9` completed PR-head workflow/job `33577174521` / `100083590564` GREEN before merge `7e119ee893801edf3e3ed3856030da78c97edfd1`, followed by GREEN `main` workflow/job `33580200835` / `100092676457`.

## Ars Zero real-distribution verification — closed by Stage 09.01

- `ENSH-L1-ARS-ZERO-REAL-FIXTURE-001` is closed. Stage 08.01 PR #59 (`feat/08-ars-zero`) established the optional registry-only provider and standalone proxy evidence while intentionally leaving actual distribution co-load open. Stage 09.01 PR #70 (`feat/09-test-matrix`) supplies the missing executable side with the actual current-pack provider chain: Ars Zero `2.0.2`, Ars Nouveau `5.13.1`, Ars Elemental `0.7.10.1`, Curios API `9.5.1+1.21.1`, GeckoLib `4.9.2` and TerraBlender `4.1.0.8`. The external JARs are fixture-only and never become normal Enshrouded implementation/runtime dependencies or production-JAR contents.
- The real fixture is isolated in `src/arsZeroGameTest` and a dedicated NeoGradle `arsZeroGameTestServer` run so optional-mod behavior cannot invalidate standalone tests that deliberately exercise provider-absent paths or synthetic players.
- `ArsZeroRealDistributionGameTests.realDistributionUsesArsZeroLichAndKeepsRewardExactlyOnce` proves exact `ars_zero:lich` resolution/construction, rejects natural/unmarked Lich story authority, proves the real provider is preferred for the marked Enshrouded manifestation, preserves encounter identity, emits exactly one authentic Enshrouded Lich Skull, and rejects replayed death callbacks from duplicating the reward.
- Final Stage 09.01 implementation HEAD `2db41c3569409beec11a6509c2c39fbfc7810a83` completed exact PR-head workflow/job `33986822592` / `101361842946` GREEN across the full gate set, including the isolated real-distribution profile. PR #70 merged as `6e957bd0592723cc4849f2a4606222ad564c2aa4`; independent post-merge `main` workflow/job `33989419851` / `101368854913` then completed the same full gate set GREEN. Both sides of the original verification boundary are therefore executable and merged.

## Closure rule

A pending contract is removed from the open list only after the merge that provides executable verification for both sides of the boundary. Closed contracts remain recorded above for provenance. If one side is implemented first, the owning task remains open or records the exact external blocker rather than being marked complete prematurely.